extends layout

block content
  .form-container
    form(action="/create" method="POST" autocomplete="off")
      input(type="text" name="name" placeholder="予定の名前")
      br
      textarea(name="detail", cols="30", rows="10" placeholder="予定の詳細")
      br
      button(type="submit") 作成